{% extends 'base.html.twig' %}

{% block title %}Campus{% endblock %}

{% block body %}
    <h1>Campus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ campus.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ campus.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_admin_campus_index') }}">Retour</a>

    <a href="{{ path('app_admin_campus_edit', {'id': campus.id}) }}">Editer</a>

    {{ include('admin/campus/_delete_form.html.twig') }}
{% endblock %}
