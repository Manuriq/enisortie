{% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary mb-2 mt-2">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
