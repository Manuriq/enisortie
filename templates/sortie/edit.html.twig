{% extends 'base.html.twig' %}

{% block title %}Editer une sortie{% endblock %}

{% block body %}
    <h1>Editer une sortie</h1>

    {{ include('sortie/_form.html.twig', {'button_label': 'Editer'}) }}

    <a class="btn btn-primary" href="{{ path('app_sortie_index') }}">Retour</a>

    {{ include('sortie/_delete_form.html.twig') }}
{% endblock %}
