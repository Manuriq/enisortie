{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Editer son profile{% endblock %}

{% block body %}
    <h1>Editer le profile de {{ participant.prenom  }} {{ participant.nom  }}</h1>

    {{ include('participant/_form.html.twig', {'button_label': 'Update'}) }}

{% endblock %}
